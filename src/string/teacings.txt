
배운 것:



String은 그냥 +만 붙이면 뒤에 붙는다. 이미 알고 있지만 알고리즘을 만들 때 기억해내야 한다.
예를 들어 String result = ""; 만 해놓고 거기에다가 result += char[i]; 이런식으로 붙일 수 있음.
간단한 건데 놓칠 수 있음.

0이 아닌 Integer.MIN_VALUE를 사용해서도 비교를 통해 가장 큰 값을 구할 수 있음.


StringBuilder(단어).reverse().toString();으로 단어 레터 앞뒤 순서를 바꿀 수 있음.







